<!DOCTYPE html>
<html>

<head>
    <style>
        @page {
            size: A4;
            margin: 20mm;
        }

        body {
            font-family: 'Century Gothic', Arial, sans-serif;
            margin: 0;
            padding: 0;
            color: #333;
            line-height: 1.5;
        }

        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-bottom: 2px solid #0056b3;
            /* IOB Blue */
            padding-bottom: 10px;
            margin-bottom: 20px;
        }

        .logo {
            width: 80px;
        }

        .office-details {
            text-align: right;
            font-size: 10px;
            color: #666;
        }

        .footer {
            position: fixed;
            bottom: 0;
            left: 0;
            right: 0;
            text-align: center;
            font-size: 8px;
            color: #999;
            border-top: 1px solid #ddd;
            padding-top: 5px;
        }

        .content {
            font-size: 11pt;
            text-align: justify;
        }

        .watermark {
            position: fixed;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%) rotate(-45deg);
            opacity: 0.05;
            font-size: 80pt;
            color: #000;
            z-index: -1;
            pointer-events: none;
        }

        /* Guilloche pattern overlay simulation */
        .security-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: radial-gradient(circle, #0056b3 1px, transparent 1px);
            background-size: 20px 20px;
            opacity: 0.03;
            z-index: -2;
            pointer-events: none;
        }
    </style>
</head>

<body>
    <div class="security-overlay"></div>
    {{#if isDraft}}
    <div class="watermark">DRAFT</div>
    {{/if}}

    <div class="header">
        <img src="data:image/svg+xml;base64,{{logoBase64}}" class="logo" alt="IOB" />
        <div class="office-details">
            <strong>Indian Overseas Bank</strong><br>
            {{officeName}}<br>
            {{officeAddress}}<br>
            Phone: {{officePhone}} | Email: {{officeEmail}}
        </div>
    </div>

    <div class="content">
        {{{body}}}
    </div>

    <div class="footer">
        Generated by IOB Governance Platform | Ref: {{refNumber}} | Date: {{generatedAt}} | Page <span
            class="pageNumber"></span>
    </div>
</body>

</html>